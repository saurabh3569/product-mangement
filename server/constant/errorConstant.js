// ----------  Auth ----------
const INVALID_CREDENTIAL = "Invalid credentials";
const NO_TOKEN = "No token, authorization denied";
const INVALID_TOKEN = "No token, authorization denied";
const USER_NOT_FOUND = "User not found";
const USER_EMAIL_CONFLICT = "User with this email already exist";

// ----------  Product ----------
const PRODUCT_NOT_FOUND = "Product not found";

module.exports = {
  INVALID_CREDENTIAL,
  NO_TOKEN,
  INVALID_TOKEN,
  USER_NOT_FOUND,
  USER_EMAIL_CONFLICT,
  PRODUCT_NOT_FOUND,
};
